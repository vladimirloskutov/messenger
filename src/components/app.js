function app() {
    const path = location.pathname;
    const root = document.getElementById('root');
    let page;

    switch (path) {
        case '/':
            break;
        default:
            break;
    }

    return root.appendChild(page);
}

export { app };
